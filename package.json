{
  "name": "bitrix24-counterparty-check",
  "version": "1.0.0",
  "private": true,
  "description": "Проверка контрагентов по ИНН через Checko для Bitrix24 CRM",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build -w shared && npm run build -w frontend && npm run build -w backend",
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
